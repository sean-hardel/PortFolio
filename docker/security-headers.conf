# Inclus dans chaque location : nginx n'hérite pas des add_header du bloc parent.

# L'export statique de Next injecte des scripts inline (hydratation) et le thème
# est appliqué par un petit script inline : 'unsafe-inline' reste nécessaire.
add_header Content-Security-Policy "default-src 'self'; script-src 'self' 'unsafe-inline'; style-src 'self' 'unsafe-inline'; img-src 'self' data:; font-src 'self'; connect-src 'self'; object-src 'none'; base-uri 'self'; form-action 'self'; frame-ancestors 'none'" always;
add_header X-Content-Type-Options "nosniff" always;
add_header X-Frame-Options "DENY" always;
add_header Referrer-Policy "strict-origin-when-cross-origin" always;
add_header Permissions-Policy "camera=(), microphone=(), geolocation=()" always;
add_header Cross-Origin-Opener-Policy "same-origin" always;

# Pris en compte uniquement derrière HTTPS (reverse proxy, Tailscale Funnel…)
add_header Strict-Transport-Security "max-age=31536000; includeSubDomains" always;
